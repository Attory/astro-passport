# APT agent instructions

## SHARED ASTRO ARCHITECTURE

Canonical cross-project documentation:

Repository:
Attory/astro-architecture

Local checkout:
/home/soul/astro-architecture

Documentation:
/home/soul/astro-architecture/docs

Before modifying a cross-service contract or ecosystem architecture, inspect
the applicable AAC documentation.

Do not modify AAC-owned contracts from a child project without an explicitly
approved AAC change.

Canonical extraction baseline: `59500ec0951e082dd4c3984999b8a42fe4ce53c8`.
Private AAC access is governance for ecosystem maintainers, never a build/test dependency for
public contributors. Public build and review must work with this repository alone.

The approved extraction branch starts at `dae015fd069308e408280d980e10ff72ed722488`.
Scientific extraction is restricted to the 16 hashed origins in `docs/extraction/origins.json`.
Preserve accepted contract 1.0.0 revision 2 and encoded profile/domain identifiers, including
their `-proposed` suffixes. They are accepted bytes, not permission to rename the encoding.
Require zero-tolerance same-platform HTTPS parity, complete leaf classification and separate
independent audits before requesting ACE cutover approval. No deployment or cutover is authorized.
Never copy private history, compatibility methodology, scoring, identities, participant/sex
logic or real birth data. API clients are ordinary independent network clients, not imports.
No request/response bodies, credentials or personal data in logs, exceptions or public fixtures.
Use synthetic public fixtures authored here. Do not copy/redact a private corpus.

Run `make check`, `uv lock --check`, `git diff --check` and exact-head CI before independent review.
Do not claim future scientific parity or AGPL activation verification has passed.
