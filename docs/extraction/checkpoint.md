# Scientific extraction — implementation checkpoint

Authority: explicit human scientific-extraction approval after AAC PR #2.
Canonical merged AAC baseline: `59500ec0951e082dd4c3984999b8a42fe4ce53c8`.
AAC merge parents: `aa4373b5d7b2539adf5bc87c0b4cc7d392d2135d` and
`7df4d66f4fdce2e6d4df80167825486d339ea7be`. AAC main was clean/synchronized after merge.

APT branch: `codex/apt-scientific-extraction-v1`, based on
`dae015fd069308e408280d980e10ff72ed722488`. ACE reference remains
`18a3776bc1ab1dc52212b4de48a0df36709d72d2`, unmodified. No deployment/cutover authorized.

## Contract and source boundary

The reviewed contract 1.0.0 revision 2 bytes are retained in `contracts/accepted/schema.json`
and identically at `contracts/astropassport/v1/schema.json`. The shape check compares every
runtime model definition with the accepted schema; it does not regenerate authority.
`contracts/accepted/semantics.md` and `acep1-profile.md` retain reviewed historical wording
and encoding identifiers. Their old proposal labels do not undo the explicit AAC acceptance.
The `-proposed` domain/profile bytes remain unchanged, as required by that acceptance.
No canonical schema tag/publication or alternative schema licence is created by this checkpoint.

`origins.json` accounts for all 16 authorized ACE origins. Thirteen have narrowly adapted
destinations; three are not copied. The raw input model is only an internal civil parser:
the service retains the actual wire-selected values and query without trimming/re-querying.
The old selected-snapshot helper and ACE Western wrapper identities are omitted. Raw natal
validation remains, without compatibility, scoring, participant/sex, identity or social code.
Acquisition uses public compliance-lock sources, never private ACE release credentials.

Scientific algorithms, UTC/UT1 approximation, native flags, binary64, nine-decimal half-even
policy, library/data versions and limitations remain unchanged. Source repository/revision
and the accepted public TBB retention-manifest hash are explicit identity substitutions.

## Execution and public evidence

`scripts.scientific_artifacts` checks pinned public sources and reproducibly builds the exact
470044-byte TZif archive. Artifact downloads are explicit build operations, never request work.
Readiness stays false without explicit configuration, immutable source SHA, validated artifacts,
exact native/runtime identity and a successful bounded isolated-worker startup check.
The default API remains disabled. Native jobs remain bounded after HTTP cancellation.
The service neither imports nor reads an ACE checkout and makes no provider calls.

The public corpus contains independently authored artificial inputs, not copied/redacted user
records. Expected scientific values were produced through the unchanged exact ACE image with
network disabled, non-root/read-only execution, memory/process limits, no core dumps and no
container swap. The reference replay was checked before exporting only mapped public scientific
fields. Private pair manifests/methodology are not published. Public expected source revisions
use an explicit zero-SHA substitution slot, never a claimed running APT build.

The public corpus's expected-value derivation uses `conformance.comparator.project_reference`;
all public mapping, encoding, artifact-build and test implementation is present in APT.
Public contributors can run every public gate without ACE or private AAC access. The private
reference driver is additional migration evidence, not required to build or modify APT.
Golden agreement demonstrates migration equivalence, not independent astronomical accuracy.

## Remaining checkpoint work

Actual authenticated local HTTPS parity, source/image verification and a green exact-head CI
are now recorded by checkpoint in `ledger.md`. Reviews A–E completed with nonblocking follow-ups;
F, G and final H remain mandatory, followed by final exact-head validation. No final readiness
claim is made. Old ACE-owned pair-manifest recomputation after API-client cutover is a separate later
gate; old digests are retained and checked now, never mislabeled as new APT fingerprints.
No VPS connection or activation is performed. This checkpoint is not final acceptance.

Schema evolution invariant: any future added/removed scientific leaf or changed error requires
an approved comparator/error-map update. Exact actual old/new leaf sets are checked, not merely
row totals. Current finite-corpus agreement cannot certify every future schema or runtime.
Zero mismatches includes explicitly scoped retention/absence/non-comparability checks; see the
precise row-count breakdown in `review-dispositions.md`, not a claim of full old pair-identity equality.
