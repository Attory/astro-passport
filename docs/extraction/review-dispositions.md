# Focused extraction review dispositions

Reviewed target: `3617ab0c208cbd56927e0b84574b778e832ca8a1`.
Exact-head CI: [35157967778](https://github.com/Attory/astro-passport/actions/runs/35157967778), both jobs successful.
Complete manifests, CLI outputs and reports: `../reviews/extraction/3617ab0c208c/`.
Five completed tool-disabled reviews A–E returned **APPROVE WITH NONBLOCKING FOLLOW-UPS**.
F and G returned only a session-limit error, not a review. They and final H remain mandatory.
No approval is claimed for unreviewed later behavior. This is not cutover/deployment acceptance.

## A — source/public-private boundary

- Origin mapping: its hash is now pinned in governance documentation; all 16 original source
  hashes were verified locally against separately accepted AAC/ACE, not merely their public headers.
- Known-import denylist: strengthened to a positive first-party scientific import boundary.
- Missing build/compliance packet material: supplied separately to G and final H; no private sources
  need disclosure. A's narrower packet omission is a review-coverage limitation, not missing repo code.
- Literal host-path test remains a supplementary tripwire, not a sandbox or comprehensive secrecy proof.
- Exact final image/parity/source evidence remains required; prior checkpoint evidence is not relabeled.

## B — boundary/civil-time

- Duplicated reviewed pins: retained to preserve the exact extracted science. Add cross-pin tests
  instead of changing scientific module layout; any dataset rotation still requires scientific review.
- Final model validation exception: a hypothetical invariant failure still reaches the existing fixed
  privacy-safe 500 boundary, never success/raw diagnostics. No actual failing input was identified.
  A stage-specific taxonomy change is deferred until demonstrated, not invented for this extraction.
- Additional Northern-Hemisphere/fractional-offset geography: useful nonblocking corpus extension;
  retained as a follow-up. Current required Russian/Australian/fold/gap/geography coverage is measured.

## C — Swiss/runtime

- Parent/worker binary identity: added direct size/hash consistency test.
- Module-wide capacity: deliberately retained. Two native workers across **all** instances in one
  process is the safe invariant; per-instance limits would allow multiplication. Deployment plan
  explicitly requires one process and a new quota/capacity review before scaling.
- OS limits: real child-process tests now exercise installed hard/soft limits, memory allocation
  refusal, CPU termination and regular-file output cap. No native astronomical result is fabricated.
- Network boundary: documented as container-enforced; subprocess environment/rlimits are not egress
  isolation. Local standalone execution must not be mislabeled an OS network sandbox.

## D — canonicalization

- Python intentionally exercises production encoder; Node/BigInt is the independent implementation.
  No second independent Python implementation is claimed or needed by the accepted two-runtime gate.
- Generic control-character escape expansion remains a gate before a future Unicode-bearing content
  profile. Current scientific content vocabulary is closed ASCII; accepted vectors remain unchanged.
- E7 runtime coordinates use `Decimal(str(binary64))`; bounded decimal-string primitive vectors are
  not an extra restriction on valid wire coordinates. Existing comments/specification retain this.
- Signed-zero diagnostic equality is not the scientific oracle: exact canonical bytes and digests are.
- Identical parity counts across checkpoints reflect the same corpus/classification map, not proof of
  no code change. In particular 94c8103 added retained civil replay; 3617ab0 changed only test portability.

## E — API/security

- Exact-peer/Unix-socket proxy trust and ingress framing/global abuse controls remain explicit
  **pre-deployment** gates. Extraction uses actual local certificate-verified direct HTTPS only.
- Admission during auth/quota is bounded and intentional; invalid credentials still cause zero science.
  This is now explicit in security documentation. Do not move auth outside bounded admission casually.
- Content-length mismatch handling is bounded by streaming size/deadline; no demonstrated defect.
- No public activation, credential provisioning or VPS changes occurred.

## Additional local comparator repair

The error comparator now pins all 46 accepted classification rows and tests every direct code/status,
retained-boundary dispatch, unavailable-with-underlying-stage evidence, malformed envelopes and
unclassified errors. The private HTTPS runner compares failures against **fresh old-stage evidence**
as well as the immutable public golden. No error taxonomy or normative contract bytes changed.
This behavior requires a fresh F review; successful parity alone does not replace it.
