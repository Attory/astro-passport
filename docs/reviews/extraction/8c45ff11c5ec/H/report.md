# Independent Review — Lens H (Whole-Extraction Composition, A–G)

**Target:** `8c45ff11c5ecb13f9e270a8aa5f670cc53e5e2b9` | **Base:** `dae015fd069308e408280d980e10ff72ed722488` | **AAC baseline:** `59500ec0951e082dd4c3984999b8a42fe4ce53c8` | **Contract checkpoint:** `75092aac79c39c6893900a0758ab176a47fe0c7a` (human-approved, treated as resolved)
**Exact-head CI:** run `35207611341`, `conclusion: success`, `headSha` matches target — treated as attested evidence, not reproduced by me.

This is a composition review only. I performed no execution. I re-examined the committed A–G reports and the current committed source/tests/docs together, looking specifically for interactions where an individually nonblocking finding becomes material once combined with another lens's finding, and for anything visible only at whole-repository scope.

---

## Composition-specific findings

### HIGH — No ledger/evidence entry exists for the exact reviewed target
**File(s):** `docs/extraction/ledger.md`, `docs/extraction/review-dispositions.md`, `docs/extraction/https-parity-*.json`, `docs/extraction/image-source-*.json`, `docs/extraction/source-bundle-*.json`
The most recent documented checkpoint in every governance/evidence artifact supplied to me is `d5d659ce44dab90049019d0df9f8f848dd2fed33` (F and G reports, dedicated `https-parity-d5d659c.json` / `image-source-d5d659c.json` / `source-bundle-d5d659c.json`). The exact target for this Lens H review, `8c45ff11c5ecb13f9e270a8aa5f670cc53e5e2b9`, has **no ledger entry, no dedicated parity/image/source-bundle receipt, and no disclosed diff summary** anywhere in the supplied material. I have no way to confirm from the record what changed between `d5d659c` and `8c45ff1`, whether any scientific/comparator/contract file differs, or whether the 23-case zero-mismatch parity result still applies at this exact commit.
**Impact:** Every prior lens's approval (A–G) was scoped to an earlier exact SHA. Composition of those approvals onto `8c45ff1` is an inference, not a directly evidenced fact. Nothing in the file contents I was given suggests scope creep, a contract-byte change, or a comparator change — all hash-pinned artifacts (`schema.json`, `comparator.json`, `errors.json`, `tbb-manifest.json`, `semantics.md`, `acep1-profile.md`) read identically to the previously reviewed versions, and `-proposed` suffixes, zero-epsilon numeric policy, and the 16-origin extraction boundary all remain intact in the code shown. This is consistent with `8c45ff1` being a small, non-scientific follow-up commit (e.g., adding this review lens's own bookkeeping), but I cannot confirm that from the record alone.
**Repair (bounded, before any final-readiness claim):** Commit a `docs/extraction/ledger.md` entry, `https-parity-8c45ff1.json`, `image-source-8c45ff1.json`, and (if the source bundle changed) `source-bundle-8c45ff1.json` for the exact reviewed SHA, following the established per-checkpoint pattern, or explicitly document in the ledger that `8c45ff1` is a no-op/documentation-only commit over `d5d659c` and cite the diff. This does not require reopening any human gate or re-running A–G; it is a record-keeping completion, not a code fix.

### MEDIUM — Composed trust surface is anchored entirely in self-referential, in-repo hash pins
**Files:** `tests/test_extraction_boundary.py` (`test_exact_accepted_contract_and_manifest_bytes`, origins-mapping hash), `conformance/comparator.py` (`MAP_SHA`, `ERROR_MAP_SHA`), `scripts/schema.py` (`ACCEPTED_SHA256`), `docs/governance.md` (origins-mapping hash)
Individually, each lens (A: origin-mapping hash; D/F: contract/comparator/error-map hash pins) treats its own hash pin as a strong integrity guard, and each is correct in isolation. In composition, all of these guards are **self-referential**: the "known-good" hash and the artifact it protects are committed together in the same repository, checked by the same CI, and could in principle be updated coherently in a single PR by the same actor who would need to violate the human-approved scope. No lens alone, and no combination of A–G, supplies an *externally anchored* attestation (e.g., a signed release tag, a third-party notarization, or an independent mirror) that the pinned bytes match what was actually human-approved at `75092aac79c3...`. This is not a new code defect; it is the composed shape of the existing control set once viewed as a whole.
**Impact:** Low practical risk given the explicit external human sign-off gates already required by the process (AAC PR review, contract checkpoint, per-review human dispatch), but worth naming explicitly at the whole-extraction level rather than leaving it implicit in five separate reports.
**Repair (nonblocking):** Consider publishing the accepted contract/comparator/error-map/origins-mapping hashes in a location outside this repository's own commit history (e.g., the AAC repository's own record of checkpoint `75092aac...`), so a compromised or mistaken single-repo commit cannot silently redefine both an artifact and its own verification hash together. Not required before this review's approval.

### RESOLVED IN COMPOSITION — Two of C's earlier nonblocking findings are already fixed downstream
**Files:** `tests/test_worker_resources.py`, `app/science/ephemeris/identity.py`, `app/science/ephemeris/worker.py`
Lens C (at `3617ab0c`) flagged (i) no cross-check that `identity.BINARY_SHA256 == worker.BINARY_SHA256`, and (ii) declared OS resource limits (`RLIMIT_CPU`, `RLIMIT_AS`, `RLIMIT_FSIZE`, etc.) were never exercised by a real subprocess test. At the state reviewed here, `tests/test_worker_resources.py::test_isolated_worker_and_parent_native_identity_agree` directly asserts the hash/size equality, and `test_actual_worker_main_installs_all_hard_and_soft_limits` / `test_actual_worker_address_space_limit_rejects_allocation` / `test_actual_worker_cpu_limit_terminates_runaway_process` / `test_actual_worker_file_size_limit_bounds_regular_file_output` run the real worker `main()` in disposable OS subprocesses and assert the limits are load-bearing (memory allocation refused, CPU-limited process killed, file output truncated). Both C findings are now closed by evidence, not merely by argument.
**No repair needed.** Noted here so the two items are not re-litigated as open in any future composition pass.

### PERSISTS, DOES NOT ESCALATE — C's module-level capacity semaphore remains an unenforced invariant
**File:** `app/science/ephemeris/adapter.py` (`_CAPACITY = threading.BoundedSemaphore(2)` at module scope)
Confirmed still present at this target, contrasted with `PassportScience`'s correctly instance-scoped `self._capacity` in `app/science/pipeline.py`. Composed with E's admission-control review and the deployment plan's "single uvicorn worker, one `PassportScience` instance" requirement, this remains a **correct-by-convention, not correct-by-construction** invariant: nothing in code prevents a future second `SwissEphemeris` instantiation from silently sharing native-process quota with an unrelated instance. It does not escalate in composition because (a) `app/main.py`'s `create_app` constructs at most one `PassportScience` per process lifespan, and (b) `docs/deployment-plan.md` already explicitly mandates single-worker deployment and flags any scaling change as a new capacity-review gate.
**Repair (nonblocking, unchanged recommendation from C):** Move `_CAPACITY` into `SwissEphemeris.__init__` as an instance attribute.

### PERSISTS, DOES NOT ESCALATE — B's tzdb final-construction exception gap is contained by API's catch-all
**Files:** `app/science/civil/tzdb.py::PinnedCivilTimeResolver.resolve` (final `return ResolvedCivilTime(...)` outside the local try/except), `app/api.py` (bare `except Exception: return error(500, "internal_error")`)
B correctly identified that a hypothetical Pydantic `ValidationError` from the final `ResolvedCivilTime` construction would not be caught by the narrower `(ValueError, KeyError, OSError)` clause and would propagate as an unmapped exception through `PassportScience._calculate`. Composed with the API layer, `app/api.py`'s outermost handler in `install_api` catches any bare `Exception` and returns a generic `500 internal_error` with **no traceback, locals, or diagnostic content** — confirmed by `test_api.py`'s repeated assertions that no secret/query/date content ever appears in response bodies or logs on any error path. So even in the untested failure mode B describes, the system fails closed and privacy-safely; it does not leak information or return a false success. This composition confirms B's finding remains LOW, not elevated by contact with the API layer.
**Repair (nonblocking, unchanged from B):** Add the defensive `try/except` and one forcing regression test for completeness/clarity, not because a leak was found.

### OBSERVATION — F's "zero mismatches" scope caveat remains accurate and unresolved as pure documentation
**Files:** `docs/extraction/ledger.md`, `docs/extraction/https-parity-*.json`
F's finding that reported "zero mismatches in all seven classes" undercounts what fraction of comparisons are true value-equality checks (vs. structural/absence/non-comparable checks) is still valid at this composition pass: the ledger and JSON evidence files still report only per-classification comparison/mismatch counts, without the true-equality proportion F recommended adding. This is unchanged by any other lens and remains a documentation-clarity item, not a comparator defect (the comparator's own code — reviewed in F — already treats absence/non-comparable rows as structural-only, not disguised equality claims).
**Repair (nonblocking, unchanged from F):** Add one summary sentence to the ledger quantifying true-equality vs. structural-only comparison counts.

### OBSERVATION — G's native-binary correspondence depth remains uneven, unaffected by later commits
**Files:** `compliance/audit.py`, `compliance/README.md`
G's finding that GCC/OpenBLAS/Rust chains get byte-level ELF-section proofs while GEOS/Shapely and pysweph/Swiss correspondence rest on source-file hashing plus a build-workflow version string (not a compiled-artifact proof) is unchanged at this target — `compliance/audit.py` still performs the same asymmetric checks. `docs/extraction/review-g-supplementary.json` (read-only, one-time evidence, explicitly scoped as non-automated) supplements but does not close this gap in the automated `compliance.audit` pipeline itself.
**Repair (nonblocking, unchanged from G):** Add ELF section/build-id checks for the compiled `libgeos_c` inside the Shapely wheel and the Swiss `.so` inside the pysweph wheel, mirroring the existing GCC-chain `compare_sections` pattern.

### OBSERVATION — A's evidence-coverage gap is closed in this composition
Lens A (reviewing only `3617ab0c`'s narrower packet) flagged that `compliance/bundle.py`, `compliance/audit.py`, `compliance/release.py`, `scripts/scientific_artifacts.py`, `scripts/https_scientific_smoke.py`, and `scripts/container_smoke.py` were not in A's material. All of these files are present and reviewable in the material supplied for this Lens H pass, and their content is consistent with what A's and G's later inferences assumed (public-only HTTPS acquisition, no private-repo dependency, no execution of untrusted archives, explicit `scientific_execution: false`). This closes A's MEDIUM finding at the whole-extraction level.
**No repair needed.**

### OBSERVATION — No lens's forbidden-scope boundary was crossed anywhere in composition
Cross-checking every lens together: no file introduces ACE modification, no deployment/activation code path exists (`docker`/CI never pushes an image or connects to a VPS; `deployment.md` and every evidence JSON say `"deployment": "not performed"` / `"not authorized"`), no canonical schema tag is published (`contracts/manifest.json: "release_git_sha": null`), no private methodology/scoring/synastry/participant/sex field appears anywhere in `app/science/**` or the contracts, and the `-proposed` encoding suffixes are byte-identical everywhere they are hash-checked. The 16-origin extraction boundary (`docs/extraction/origins.json`) is unchanged in shape and content across every reviewed revision. I found no place where combining lenses reveals a forbidden action that no single lens noticed alone.

---

## Evidence limitations (stated explicitly)

- I executed nothing. All CI, parity, image/source-correspondence, and prior-lens conclusions are treated as attested records supplied to me, not independently reproduced.
- The single largest gap in this composition pass is structural, not textual: the exact target `8c45ff11c5ecb13f9e270a8aa5f670cc53e5e2b9` has no dedicated ledger/evidence trail in the supplied material, unlike every prior checkpoint. My conclusion that the visible file contents are consistent with prior approvals is a content-comparison inference, not a verified diff against `d5d659c`.
- I cannot independently verify the private-side SHA-256 provenance claims in `app/science/**` headers against the real ACE repository at `18a3776bc1ab1dc52212b4de48a0df36709d72d2` — this remains, as in every prior lens, outside what a public-only reviewer can check, by design.
- I have no ability to verify astronomical correctness of any golden value; only migration/encoding self-consistency was checked, as in F.
- This is a technical composition review, not a legal AGPL compliance certification, not a security audit, and not independent execution of any test, build, or CI step.

## Implemented extraction/parity vs. forbidden future items — explicit separation

**Implemented and reviewed as committed fact, across all lenses in composition:** 16-origin scientific extraction with per-file provenance hashes; hash-pinned accepted contract/comparator/error-map/ACEP1-profile bytes with `-proposed` suffixes intact; zero-epsilon binary64/canonical numeric policy; fail-closed API/security scaffold with bounded admission, quota, and privacy controls; isolated, resource-limited native Swiss subprocess with fallback/warning refusal, now backed by real-subprocess resource-limit tests; deterministic, twice-reproduced Corresponding Source bundle with native/OS correspondence auditing; non-root/read-only/no-capability container build; exact-head CI success recorded for the reviewed target.

**Not implemented, not claimed, and correctly absent from every reviewed file:** ACE modification or removal; client cutover; canonical schema tag/publication; public binary/image distribution; network activation or VPS deployment; any claim of independent astronomical accuracy beyond migration equivalence; any claim of legal certification.

---

## Verdict

**APPROVE WITH NONBLOCKING FOLLOW-UPS**

Rationale: composing lenses A–G against the current material reveals no case where a previously nonblocking finding becomes CRITICAL or blocking — several in fact resolve downstream (C's binary-identity and resource-limit gaps are now test-covered; A's evidence-coverage gap is closed by this review's fuller packet), and the remainder (module-scoped capacity semaphore, tzdb defensive-exception gap, comparator zero-mismatch documentation nuance, uneven native-binary proof depth) remain low-impact and contained by other layers (the API's fail-closed catch-all, the single-instance deployment invariant, existing hash-pinned classification structure). The one genuinely new composition-level finding — the absence of a ledger/evidence entry for the exact target `8c45ff1` itself, unlike every prior checkpoint — is HIGH-priority for record completeness but is not evidence of a substantive violation: nothing in the visible file contents indicates scope creep, contract drift, forbidden content, or reopened human gates at this commit. This should be closed (ledger entry + per-target evidence receipts, or an explicit no-op annotation) before this composition is cited as a final-readiness basis, but it does not by itself require reopening or repeating reviews A–G. No deployment, ACE cutover, or ACE modification is authorized, claimed, or found anywhere in this composed material.
